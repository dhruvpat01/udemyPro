<%-include("partials/header")%>



<section class="head_nav">
  <nav class="navbar navbar-expand-lg navbar-light bg-light op">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">MyCourses</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/register">Register</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
          <%if(rol==="admin"){%>
            <li class="nav-item">
              <a class="nav-link" href="/admin">Create_Course</a>
            </li>
          <%}%>
          <li class="nav-item">
            <a class="nav-link" href="/user">User</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</section>


<section>
<% course.forEach(function(course){ %>
    <% if(name==course.course_title){%>
    <div>
        <img src="" class="pet-profile justify-content-center" alt="User Photo Here">
    </div>
    <hr>
    <div >
        Course_name: <h2><%= course.course_name %> </h2>
        <hr>
        Email: <%= course.email %> 
        <hr>
        course_title:<%= course.course_title %> 
        <hr>
        course_description:<%= course.course_description %> 
        <hr>
        duration:<%= course.duration %> 
        <hr>
        start_date:<%= course.start_date %> 
        <hr>
        <hr>
        end_date:<%= course.end_date %> 
        <hr>
        <hr>
        min:<%= course.min %> 
        <hr>
        <hr>
        max:<%= course.max %> 
        <hr>
        <% if(rol==="student"){ %>
        <div class="row justify-content-center ">
            <div class="col-lg-6">
              <form id="student_id" action="/:<%= course.course_title %>" method="POST" class="registerForm">
                <input type="hidden" class="form-control" name='course_title' value="<%= course.course_title %>" >
                <input type="hidden" class="form-control" name="start_date"value="<%= course.start_date %>" >
                <input type="hidden" class="form-control" name="duration"  value="<%= course.duration %>">
                <button type="submit" formaction="/:<%= course.course_title %>" method="POST" class="btn btn-dark btn-lg button1 j registerButton"  >Enroll</button>
                
              </form>
                
            </div>
        </div>
        <% } %>
    </div>
    </div>
    
</section>
<% if(rol==="admin"){ %>
  <section>
    <h1>List of students enrolled for this course</h1>
    <ul>
      <% student.forEach(function(stud){ %>
      <li><%= stud.email %></li>
     
      <% }); %>
    </ul>
    <a  href="/pos">Send Email</a>
  </section>
<% } %>
<% }%>
    <% }); %>
   
    <script src="/app.js"></script>
    

</body>

</html>